import{Input,Select}from"../../design-system";export default function ReportFilters({filters,onChange}){const set=k=>e=>onChange({...filters,[k]:e.target.value});return <section className="report-filters"><Input type="date" label="From date" value={filters.from} onChange={set("from")}/><Input type="date" label="To date" value={filters.to} onChange={set("to")}/><Select label="Category" value={filters.category} onChange={set("category")} options={[["","All categories"],["requests","Service requests"],["assets","Assets"],["repairs","Repairs"],["inventory","Inventory"]]}/><Select label="Status" value={filters.status} onChange={set("status")} options={[["","All statuses"],["open","Open"],["completed","Completed"],["pending","Pending"]]}/><Input label="Department" value={filters.department} onChange={set("department")}/></section>}
