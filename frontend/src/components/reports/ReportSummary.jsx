import{Card}from"../../design-system";export default function ReportSummary({data}){const cards=[["Total requests",data?.tickets?.total??data?.totalTickets??0],["Completed requests",data?.tickets?.completed??0],["Asset count",data?.assets?.total??data?.totalAssets??0],["Repair count",data?.repairs?.total??data?.totalRepairs??0],["Inventory status",data?.inventory?.total??data?.totalInventory??0]];return <div className="report-summary">{cards.map(([l,v])=><Card key={l}><span>{l}</span><strong>{v}</strong></Card>)}</div>}
