import{Card}from"../../design-system";export default function ReportSummary({data,loading}){const display=(value)=>loading?"…":value==null?"Unavailable":value;const cards=[["Total requests",data?.tickets?.total],["Completed requests",data?.tickets?.completed],["Asset count",data?.assets?.total],["Repair count",data?.repairs?.total],["Inventory items",data?.inventory?.totalItems]];return <div className="report-summary" aria-busy={loading}>{cards.map(([label,value])=><Card key={label}><span>{label}</span><strong>{display(value)}</strong></Card>)}</div>}
