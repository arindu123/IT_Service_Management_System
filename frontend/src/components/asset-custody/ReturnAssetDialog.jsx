import{ConfirmationDialog}from"../../design-system";import{assetLabel,personLabel}from"./custodyUtils";export default function ReturnAssetDialog({issue,onClose,onConfirm,loading}){return <ConfirmationDialog open={Boolean(issue)} onClose={onClose} onConfirm={onConfirm} loading={loading} title="Return asset" message={issue?`Confirm return of ${assetLabel(issue.item)} from ${personLabel(issue.user,issue.userSnapshot)}.`:""} confirmLabel="Confirm return"/>;}
