import{Input,Select}from"../../design-system";export default function CustodyFilters({filters,onChange}){const change=(key)=>(event)=>onChange({...filters,[key]:event.target.value});return <section className="custody-filters" aria-label="Custody filters"><Input label="Search" type="search" value={filters.search} onChange={change("search")} placeholder="Employee, asset ID, serial number or officer"/><Select label="Status" value={filters.status} onChange={change("status")}><option value="">All statuses</option>{["issued","returned","transferred","destroyed"].map((value)=><option key={value} value={value}>{value.replace(/_/g," ")}</option>)}</Select></section>;}
