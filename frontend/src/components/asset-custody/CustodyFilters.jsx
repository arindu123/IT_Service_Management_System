import{Input,Select}from"../../design-system";import{useTranslation}from"../../i18n/LanguageContext";
export default function CustodyFilters({filters,onChange}){const{t}=useTranslation();const change=(key)=>(event)=>onChange({...filters,[key]:event.target.value});return <section className="custody-filters" aria-label={t('custodyPage.assetCustodyRecords')}><Input label={t('custodyPage.searchLabel')} type="search" value={filters.search} onChange={change("search")} placeholder={t('custodyPage.searchPlaceholder')}/><Select label={t('labels.status')} value={filters.status} onChange={change("status")}><option value="">{t('custodyPage.allStatuses')}</option>{["issued","returned","transferred","destroyed"].map((value)=><option key={value} value={value}>{value.replace(/_/g," ")}</option>)}</Select></section>;}
