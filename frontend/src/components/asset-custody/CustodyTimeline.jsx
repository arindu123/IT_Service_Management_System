export default function CustodyTimeline({issue,formatDateTime}){const items=[{label:"Asset issued",date:issue.issueDate,actor:issue.issuedBy?.name||issue.issuedBySnapshot?.name},{label:"Asset transferred",date:issue.transferDate,actor:issue.transferredBy?.name||issue.transferredBySnapshot?.name},{label:"Asset returned",date:issue.returnDate,actor:issue.returnedBy?.name||issue.returnedBySnapshot?.name},{label:"Asset destroyed",date:issue.destroyDate,actor:issue.destroyedBy?.name||issue.destroyedBySnapshot?.name}].filter((item)=>item.date);return <ol className="custody-timeline">{items.map((item)=><li key={item.label}><span aria-hidden="true"/><div><strong>{item.label}</strong><p>{item.actor||"System"} · {formatDateTime(item.date)}</p></div></li>)}</ol>;}
