import{useTranslation}from"../../i18n/LanguageContext";
export default function CustodyTimeline({issue,formatDateTime}){const{t}=useTranslation();const items=[{label:t('custodyPage.assetIssued'),date:issue.issueDate,actor:issue.issuedBy?.name||issue.issuedBySnapshot?.name},{label:t('custodyPage.assetTransferred'),date:issue.transferDate,actor:issue.transferredBy?.name||issue.transferredBySnapshot?.name},{label:t('custodyPage.assetReturned'),date:issue.returnDate,actor:issue.returnedBy?.name||issue.returnedBySnapshot?.name},{label:t('custodyPage.assetDestroyedMsg'),date:issue.destroyDate,actor:issue.destroyedBy?.name||issue.destroyedBySnapshot?.name}].filter((item)=>item.date);return <ol className="custody-timeline">{items.map((item)=><li key={item.label}><span aria-hidden="true"/><div><strong>{item.label}</strong><p>{item.actor||t('custodyPage.systemLabel')} · {formatDateTime(item.date)}</p></div></li>)}</ol>;}
