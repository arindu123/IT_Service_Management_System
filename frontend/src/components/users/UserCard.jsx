import { Card,Button } from "../../design-system"; import RoleBadge from "./RoleBadge"; import UserStatus from "./UserStatus"; export default function UserCard({user,onView}){return <Card className="user-card"><div className="user-card-head"><strong>{user.name}</strong><UserStatus user={user}/></div><p>{user.email}</p><dl><div><dt>Employee ID</dt><dd>{user.employeeId||"—"}</dd></div><div><dt>Role</dt><dd><RoleBadge role={user.role}/></dd></div><div><dt>Department</dt><dd>{user.department||"—"}</dd></div></dl><Button variant="ghost" onClick={()=>onView(user)}>View details</Button></Card>;}
