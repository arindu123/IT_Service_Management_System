import { Modal } from "../../design-system"; import RoleBadge from "./RoleBadge"; import UserStatus from "./UserStatus"; export default function UserDetails({user,open,onClose}){if(!user)return null;return <Modal open={open} onClose={onClose} title={user.name} size="large"><div className="user-details-grid"><div><small>Profile information</small><strong>{user.name}</strong><span>{user.email}</span><span>Employee ID: {user.employeeId||"—"}</span></div><div><small>Account information</small><UserStatus user={user}/><span>Department: {user.department||"—"}</span><span>Last activity: {user.lastLogin?new Date(user.lastLogin).toLocaleString():"—"}</span></div><div><small>Assigned role</small><RoleBadge role={user.role}/></div><div><small>Permissions summary</small><span>Access is governed by the assigned system role.</span></div></div></Modal>;}
