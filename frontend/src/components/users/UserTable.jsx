import { Button,DataTable } from "../../design-system"; import RoleBadge from "./RoleBadge"; import UserStatus from "./UserStatus"; import UserCard from "./UserCard"; const cols=[{key:"id",header:"Employee ID"},{key:"name",header:"Name"},{key:"email",header:"Email"},{key:"department",header:"Department"},{key:"role",header:"Role"},{key:"status",header:"Status"},{key:"actions",header:"Actions"}]; export default function UserTable({users,loading,onView,onRoleChange,saving}){const cell=(u,c)=>({id:u.employeeId||"—",name:<strong>{u.name}</strong>,email:u.email,department:u.department||"—",role:<div className="user-role-cell"><RoleBadge role={u.role}/><select aria-label={`Change role for ${u.name}`} value={u.role} disabled={saving===u._id} onChange={e=>onRoleChange(u,e.target.value)}>{["admin","system_admin","head_of_it","technician","department_user","store_keeper","procurement_officer","management"].map(r=><option key={r} value={r}>{r.replace(/_/g," ")}</option>)}</select></div>,status:<UserStatus user={u}/>,actions:<Button variant="ghost" onClick={()=>onView(u)}>View</Button>}[c.key]);return <><div className="users-table-desktop"><DataTable columns={cols} data={users} loading={loading} rowKey="_id" caption="User accounts" emptyTitle="No users found" renderCell={cell}/></div><div className="users-card-list">{!loading&&users.map(u=><UserCard key={u._id} user={u} onView={onView}/>)}</div></>;}
