import{Button,Card,CardBody,CardHeader}from"../../design-system";
export default function AssetActions({asset,canManage,onEdit,onCustody,onRepair,onDestroy}){const destroyed=asset.status==="destroyed";return <Card><CardHeader title="Actions"/><CardBody><div className="asset-action-list"><Button onClick={onEdit}>Edit</Button>{canManage&&!destroyed&&<Button variant="secondary" onClick={onCustody}>{asset.status==="issued"?"Transfer / Return":"Assign"}</Button>}<Button variant="secondary" onClick={onRepair}>Repair</Button>{canManage&&!destroyed&&<Button variant="danger" onClick={onDestroy}>Destroy</Button>}<Button variant="ghost" disabled title="No existing retire workflow is available">Retire</Button></div></CardBody></Card>;}
