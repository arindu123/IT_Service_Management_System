import{Button,Card,CardBody,CardHeader,TableEmptyState}from"../../design-system";import{invoiceExtension}from"./assetUtils";import{useTranslation}from"../../i18n/LanguageContext";
export default function AssetDocuments({asset,onPreview}){const{t}=useTranslation();const label=asset.itemNumber||asset.assetId||"asset";return <Card><CardHeader title={t('assetPage.documents')} description={t('assetPage.documentsDesc')}/><CardBody>{asset.invoiceImage?<div className="asset-document"><img src={asset.invoiceImage} alt={`${t('assetPage.invoice')} ${label}`}/><div><strong>{t('assetPage.invoiceImageLabel')}</strong><span>{t('assetPage.available')}</span></div><div><Button variant="secondary" onClick={()=>onPreview({src:asset.invoiceImage,label})}>{t('common.view')}</Button><a className="gov-button gov-button--ghost" href={asset.invoiceImage} download={`${label}-invoice.${invoiceExtension(asset.invoiceImage)}`}>{t('common.download')}</a></div></div>:<TableEmptyState title={t('assetPage.noDocumentsAttached')} description={t('assetPage.noDocumentsDesc')}/>}</CardBody></Card>;}
