import{Button,Card,CardBody,CardHeader,TableEmptyState}from"../../design-system";import{invoiceExtension}from"./assetUtils";
export default function AssetDocuments({asset,onPreview}){const label=asset.itemNumber||asset.assetId||"asset";return <Card><CardHeader title="Documents" description="Invoice, warranty and supporting records."/><CardBody>{asset.invoiceImage?<div className="asset-document"><img src={asset.invoiceImage} alt={`Invoice for ${label}`}/><div><strong>Invoice image</strong><span>Available</span></div><div><Button variant="secondary" onClick={()=>onPreview({src:asset.invoiceImage,label})}>View</Button><a className="gov-button gov-button--ghost" href={asset.invoiceImage} download={`${label}-invoice.${invoiceExtension(asset.invoiceImage)}`}>Download</a></div></div>:<TableEmptyState title="No documents attached" description="No invoice, warranty document or attachment is recorded."/>}</CardBody></Card>;}
