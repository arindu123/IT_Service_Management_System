import{Select,TableEmptyState}from"../../design-system";import DeviceStatus from"./DeviceStatus";const ranges=[{value:"24h",label:"Last 24 hours"},{value:"7d",label:"Last 7 days"},{value:"30d",label:"Last 30 days"}];export default function NetworkActivity({history,range,onRangeChange,formatDateTime}){return <section className="network-activity"><div className="network-section-heading"><h2>Status history</h2><Select label="History range" value={range} onChange={event=>onRangeChange(event.target.value)} options={ranges}/></div>{history?.length?<div className="network-history" role="table">{history.map((item,index)=><div className="network-history-row" key={item._id||index}><span>{item.checkedAt?formatDateTime(item.checkedAt):"—"}</span><DeviceStatus status={item.status}/><span>{item.responseTimeMs!=null?`${item.responseTimeMs} ms`:"No response"}</span></div>)}</div>:<TableEmptyState title="No status history" description="Checks will appear here after monitoring runs."/>}</section>}
