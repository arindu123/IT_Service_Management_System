import { Select, TableEmptyState } from "../../design-system"; import DeviceStatus from "./DeviceStatus";
export default function NetworkActivity({ history, range, onRangeChange, formatDateTime }) { return <section className="network-activity"><div className="network-section-heading"><h2>Status history</h2><Select aria-label="History range" value={range} onChange={e=>onRangeChange(e.target.value)} options={[["24h","Last 24 hours"],["7d","Last 7 days"],["30d","Last 30 days"]]}/></div>{history?.length?<div className="network-history" role="table">{history.map((item,index)=><div className="network-history-row" key={item._id||index}><span>{item.checkedAt?formatDateTime(item.checkedAt):"—"}</span><DeviceStatus status={item.status}/><span>{item.responseTimeMs!=null?`${item.responseTimeMs} ms`:"No response"}</span></div>)}</div>:<TableEmptyState title="No status history" description="Checks will appear here after monitoring runs."/>}</section>; }
