import { Button, Card } from "../../design-system"; import DeviceStatus from "./DeviceStatus"; import PingStatus from "./PingStatus";
export default function DeviceCard({ device, onView, onCheck }) { return <Card className="network-device-card"><div className="network-card-heading"><strong>{device.name}</strong><DeviceStatus status={device.status}/></div><p>{device.ipAddress || device.hostname || "No address"}</p><dl><div><dt>Type</dt><dd>{device.deviceType}</dd></div><div><dt>Location</dt><dd>{[device.building,device.room].filter(Boolean).join(" / ")||"—"}</dd></div><div><dt>Response</dt><dd><PingStatus device={device}/></dd></div></dl><div className="network-card-actions"><Button variant="ghost" onClick={()=>onView(device)}>View</Button><Button variant="secondary" onClick={()=>onCheck(device)}>Check</Button></div></Card>; }
