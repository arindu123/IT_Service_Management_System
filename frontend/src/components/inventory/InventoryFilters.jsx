import{Input,Select}from"../../design-system";import{INVENTORY_CATEGORIES}from"./inventoryUtils";export default function InventoryFilters({filters,onChange,enumLabel}){const change=(key)=>(event)=>onChange({...filters,[key]:event.target.value});return <section className="inventory-filter-bar" aria-label="Inventory filters"><Input label="Search" type="search" value={filters.search} onChange={change("search")} placeholder="Item, supplier, category or location"/><Select label="Category" value={filters.category} onChange={change("category")}><option value="">All categories</option>{INVENTORY_CATEGORIES.map((value)=><option key={value} value={value}>{enumLabel("inventoryCategory",value)}</option>)}</Select><Select label="Status" value={filters.status} onChange={change("status")}><option value="">All statuses</option><option value="available">Available</option><option value="low">Low stock</option><option value="critical">Critical stock</option></Select><Select label="Low stock filter" value={filters.lowStock} onChange={change("lowStock")}><option value="">All stock levels</option><option value="yes">Low stock only</option><option value="no">Above minimum</option></Select></section>;}
