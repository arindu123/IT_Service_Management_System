import{Button,Card,CardBody}from"../../design-system";import StockStatus from"./StockStatus";import{currency,itemStatus}from"./inventoryUtils";export default function InventoryCard({item,enumLabel,onView,onEdit}){return <Card><CardBody><div className="inventory-card-heading"><strong>{item.itemName}</strong><StockStatus status={itemStatus(item)}/></div><p>{enumLabel("inventoryCategory",item.category)}</p><dl><div><dt>Available</dt><dd>{item.quantity}</dd></div><div><dt>Minimum level</dt><dd>{item.reorderLevel}</dd></div><div><dt>Unit cost</dt><dd>{currency(item.unitPrice)}</dd></div><div><dt>Supplier</dt><dd>{item.supplierName||"Not recorded"}</dd></div></dl><div className="inventory-card-actions"><Button variant="secondary" onClick={()=>onView(item)}>View details</Button><Button variant="ghost" onClick={()=>onEdit(item)}>Edit</Button></div></CardBody></Card>;}
