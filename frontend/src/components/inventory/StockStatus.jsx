import{StatusBadge}from"../../design-system";import{useTranslation}from"../../i18n/LanguageContext";const toneMap={available:"success",low:"warning",critical:"danger"};export default function StockStatus({status}){const{t}=useTranslation();const key=`inventoryPage.${status==="available"?"available":status==="low"?"lowStock":"criticalStock"}`;const label=t(key);const tone=toneMap[status]||"neutral";return <StatusBadge label={label} tone={tone}/>;}
