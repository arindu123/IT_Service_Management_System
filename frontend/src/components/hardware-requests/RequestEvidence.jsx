import { Button, Card, CardBody, CardHeader, TableEmptyState } from "../../design-system";
import { fileSize } from "./requestConstants";

export default function RequestEvidence({ files=[], formatDate, loading, canDelete, onView, onDownload, onDelete }) { return <Card><CardHeader title="Attachments" description={`${files.length} evidence file(s)`}/><CardBody>{files.length?<ul className="request-evidence-list">{files.map((file)=><li key={file._id}><div><strong>{file.originalName}</strong><span>{fileSize(file.size)} · {formatDate(file.uploadedAt||file.createdAt)}</span></div><div><Button variant="ghost" disabled={loading} onClick={()=>onView(file)}>View</Button><Button variant="secondary" disabled={loading} onClick={()=>onDownload(file)}>Download</Button>{canDelete(file)&&<Button variant="danger" disabled={loading} onClick={()=>onDelete(file)}>Delete</Button>}</div></li>)}</ul>:<TableEmptyState title="No attachments" description="No evidence files were uploaded for this request."/>}</CardBody></Card>; }
