import{Button,Card,CardBody}from"../../design-system";import RepairStatus from"./RepairStatus";import{formatDate,formatValue,repairAsset}from"./repairUtils";export default function RepairCard({repair,onView,onEdit}){return <Card><CardBody><div className="repair-card-heading"><strong>{repair.rrNumber||repair.repairId}</strong><RepairStatus status={repair.repairStatus}/></div><h2>{repair.errorDescription||"Repair record"}</h2><p>{repairAsset(repair)} · {formatValue(repair.userName)}</p><dl><div><dt>Type</dt><dd>{formatValue(repair.type)}</dd></div><div><dt>Received</dt><dd>{formatDate(repair.receivedDate)}</dd></div><div><dt>Technician</dt><dd>{repair.technician?.name||"Not assigned"}</dd></div></dl><div className="repair-card-actions"><Button variant="secondary" onClick={()=>onView(repair)}>View details</Button><Button variant="ghost" onClick={()=>onEdit(repair)}>Edit</Button></div></CardBody></Card>;}
