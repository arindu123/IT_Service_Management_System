import{DateField,Input,Select}from"../../design-system";import{REPAIR_MODELS,REPAIR_STATUSES,REPAIR_TYPES,statusLabel}from"./repairUtils";export default function RepairFilters({filters,onChange}){const change=(key)=>(event)=>onChange({...filters,[key]:event.target.value});return <section className="repair-filter-bar" aria-label="Repair filters"><Input label="Search" type="search" value={filters.search} onChange={change("search")} placeholder="Repair ID, asset, issue or requester"/><Select label="Status" value={filters.status} onChange={change("status")}><option value="">All statuses</option>{REPAIR_STATUSES.map((v)=><option key={v} value={v}>{statusLabel(v)}</option>)}</Select><Select label="Priority" value={filters.priority} onChange={change("priority")}><option value="">All priorities</option>{["low","medium","high","critical"].map((v)=><option key={v} value={v}>{v}</option>)}</Select><Select label="Device" value={filters.device} onChange={change("device")}><option value="">All devices</option>{[...new Set([...REPAIR_TYPES,...REPAIR_MODELS])].map((v)=><option key={v} value={v}>{statusLabel(v)}</option>)}</Select><DateField label="Created date" optional value={filters.date} onChange={change("date")}/></section>;}
